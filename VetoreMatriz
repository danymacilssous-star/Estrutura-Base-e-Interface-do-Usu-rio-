programa {
  funcao inicio() {
    
  }
}
        // Vetores ativos de TI
cadeia ativos[3] = {"Notebooks", "Monitores", "Teclados"}
        // Matriz:
        // Coluna 0 = Quantidade Atual
        // Coluna 1 = Quantidade Mínima
        inteiro estoque[3][2] = {
            {5, 10},   // Notebooks - abaixo da quantidade mínima
            {15, 10},  // Monitores
            {20, 15}   // Teclados
        inteiro opcao = 0

        // Mantém o sistema funcionando até escolher a opção 3
        enquanto (opcao != 3)
