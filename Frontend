programa
{

 inicio
  cadeia = {"Notebook, Mouse, Teclado"}
  inteiro = {"quantidade de produtos",}
  inteiro = ("quantidade mínima")
  
// 3 linhas, 2 colunas
inteiro quantidade [3] [2]

Coluna 0 = (quantidade atual)
coluna 1 = (quantidade mínima)

para (inteiro i = 1; i < 3; i++)

para (inteiro j = 1; j < 2; j++)

   enquanto (quantidade atual < quantidade mínima)
   escreva (
   	enquanto (quantidade atual < quantidade minima)
   escreval("Estoque abaixo do limite mínimo!")
  
  escolha opcao
      caso 1
         escreval("Lógica do relatório entrará aqui")
      caso 2
         escreval("Lógica do recebimento entrará aqui")
      caso 3
         escreval("Encerrando o sistema...")
      outrocaso
         escreval("Opção inválida!")
   fimescolha
fimenquanto
